|| *Фундаментальный тип* || *Java класс* || *Тип базы данных (на примере Firebird)* || *Тип базы данных (на примере MS SQL Server)* ||
|| строка || String || varchar || varchar ||
|| целое число || Long || int64 || bigint ||
|| вещественное число || Double || double || real ||
|| булев тип (истина/ложь) || smallint || || tinyint ||
|| символ || Character || char || char ||
|| файл || byte[] + метаданные || ссылка на объект Attachment || ссылка на объект Attachment ||
|| набор байт неограниченного размера (или строка неограниченной длины) || byte[] || lob ||
|| дата || Date || timestamp ||  date ||
|| дата и время || Date || timestamp || datetime ||
|| пересчислимый тип/справочник/ссылка на другую сущность || enum/Object || foreign key || foreign key ||